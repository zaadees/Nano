# Dependencies for WK12 scripts
click==8.1.7
requests==2.31.0
beautifulsoup4==4.12.2
# Using a wheel-only version of lxml to avoid compilation issues
lxml==4.9.3; platform_system!="Linux"
lxml==4.9.3; platform_system=="Linux" and platform_machine!="aarch64" and platform_machine!="arm64"
# For Linux ARM systems, use a version with prebuilt wheels
lxml==4.6.5; platform_system=="Linux" and (platform_machine=="aarch64" or platform_machine=="arm64")
